# UUID de l’organisation mère plateforme (après migration fusion, = keeper en base).
# VITE_PRIMARY_ORGANIZATION_ID=

# true ou 1 : UI une seule org + pas de création de tenants (maintenance). false : tenants hébergés possibles.
# VITE_SINGLE_ORGANIZATION_MODE=false
